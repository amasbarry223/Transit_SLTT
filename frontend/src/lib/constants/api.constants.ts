/** Timeouts réseau et API (ms). */

/** Délai par défaut avant impression après ouverture de la fenêtre print. */
export const PRINT_WINDOW_READY_MS = 400;

/** Délai court après chargement des images avant impression. */
export const PRINT_IMAGE_READY_MS = 80;

/** Délai de repli si les images ne signalent pas leur chargement. */
export const PRINT_FALLBACK_MS = 2_500;
