# Voir frontend/.npmrc pour la justification complète : sans lockfile
# committé, npm doit résoudre l'arbre de dépendances en entier à chaque
# install, ce qui déclenche systématiquement sur Hostinger un bug connu de
# l'Arborist npm ("Cannot read properties of null (reading 'edgesOut')").
legacy-peer-deps=true
